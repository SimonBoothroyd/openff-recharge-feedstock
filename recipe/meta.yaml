{% set name = "openff-recharge" %}
{% set version = "0.0.1a4" %}
{% set sha256 = "00a2d2285a6059b3cd4f2c4d64caa261c660f8b071ae27e193353bbb1b5837e2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/openforcefield/{{ name }}/archive/{{ version | replace("a", "-alpha.") }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - click
    - numpy
    - pydantic
    - jinja2
    - sqlalchemy
    - sqlite
    - typing-extensions

test:
  imports:
    - openff.recharge

about:
  home: https://github.com/openforcefield/openff-recharge
  license: MIT
  license_file: LICENSE
  description: An automated framework for generating optimized partial charges for molecules.

extra:
  recipe-maintainers:
    - simonboothroyd